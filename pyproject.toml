# PyPi.org package stuff
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mcp_client_console"]

[tool.hatch.version]
path = "src/mcp_client_console/__init__.py"


# Config project stuff
[project]
name = "mcp-client-console"
dynamic = ["version"] # version dynamically pulled from __init__.py in src package
description = "MCP client that connects to a remote MCP server"
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.25,<2",
    "platformdirs>4.0",
    "httpx>=0.27,<1",
]

[project.urls]
Homepage = "https://github.com/geomux/mcp-client-console"

[project.scripts]
mcp-client-console = "mcp_client_console.cli:main"





